import Form from "./Form"

export default function Contact() {
    return (
        <section className="contact" id="contact">
            <h1>Contact Me</h1>
            <Form />
        </section>
    )
}